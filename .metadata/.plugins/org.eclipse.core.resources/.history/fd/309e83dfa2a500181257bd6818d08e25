/** 
 * 배열 관련한 공통 기능 정의 
 * @author KOSTA
 * */
public class ArrayUtil {

	/**
	 * 배열 복사 
	 *  
	 * @param src			복사하고자 하는 원본 배열
	 * @param increment		증가치
	 * @return				복사된 배열 
	 */
	public static int[] copy(int[] src, int increment) {		
		int[] array = new int[src.length + increment];
		
		for (int i = 0; i < src.length; i++) {
			array[i] = src[i];
		}
		System.out.println();
		
		for (int i : array) {
			System.out.print(i + "\t");
		}
		System.out.println();
		return null;
	}
}
